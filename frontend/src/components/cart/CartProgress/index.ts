/**
 * CartProgress Module
 * Visual cart progress and milestones
 */

export { CartProgress } from './CartProgress';
export { default } from './CartProgress';
export type {
  CartProgressProps,
  ProgressMilestone,
  CheckoutStep,
} from './CartProgress';
